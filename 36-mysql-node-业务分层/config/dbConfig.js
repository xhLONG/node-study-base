const dbConfig = {
  host: 'localhost',
  user: 'root',
  password: 'root',
  database: 'node-mysql-test',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0
}

module.exports = dbConfig